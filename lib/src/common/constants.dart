/// The default width of a widget in the dashboard.
const kWidgetWidth = 350.0;

/// The default height of a widget in the dashboard.
const kWidgetHeight = 350.0;

/// The default spacing between widgets in the dashboard.
const kWidgetSpacing = 16.0;
